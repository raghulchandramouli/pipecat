"""Local launcher overlay for the pinned Hugging Face speech-to-speech checkout.

This package is intentionally named under :mod:`HFS` so it never replaces the
installed upstream :mod:`speech_to_speech` package.  The launcher adds this
directory to the upstream package's module search path only for the two local
Sarvam handlers.
"""

